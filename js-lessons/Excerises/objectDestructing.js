// Q1 .Extract name and age using object destructuring
const user = { name: 'Ash', age: 25, country: 'India' };

// Q3.Destructure `theme` and `fontSize` with a default value for fontSize
const settings = { theme: 'dark' };

// Q4. Destructure and rename `brand` to `carBrand`
const car = { brand: 'Toyota', model: 'Camry' };

//Q5. Extract `id`, `firstName`, and `lastName` using destructuring
const response = {
  data: {
    user: {
      id: 101,
      profile: {
        firstName: 'Tom',
        lastName: 'Hardy'
      }
    }
  }
};


